<Window x:Class="SoundboardApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modern Soundboard" 
        Height="700" 
        Width="1000"
        MinHeight="500"
        MinWidth="800"
        Background="#FF1E1E1E"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Focusable="True"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF3E3E42"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF007ACC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FF404040"/>
                                <Setter Property="Foreground" Value="#FF808080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sound Button Style -->
        <Style x:Key="SoundButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF0E639C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FF1177BB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="MinHeight" Value="50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1177BB"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007ACC"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF007ACC" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF007ACC"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF007ACC" BlurRadius="12" ShadowDepth="0" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                              Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              CornerRadius="2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow"
                                                    Grid.Column="1"
                                                    Fill="#FFFFFF"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="8,3,23,3"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"
                                            TextElement.Foreground="#FFFFFF"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                          Background="#FF2D2D30"
                                          BorderThickness="1"
                                          BorderBrush="#FF3F3F46"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Border" 
                                      Background="{TemplateBinding Background}"
                                      Padding="{TemplateBinding Padding}">
                                    <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF007ACC"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF0E639C"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Style.Resources>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <!-- ProgressBar Style -->
        <Style x:Key="ModernProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FF007ACC"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF252526" Padding="20,15">
            <StackPanel>
                <TextBlock Text="Modern Soundboard" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="#FFFFFF" 
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Mix microphone audio with sound effects" 
                          FontSize="14" 
                          Foreground="#FFCCCCCC" 
                          HorizontalAlignment="Center" 
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Instructions Panel -->
        <Border Grid.Row="0" Background="#FF1E1E1E" BorderBrush="#FF3F3F46" BorderThickness="1" CornerRadius="4" Margin="20,20,20,10">
            <StackPanel Margin="15">
                <TextBlock Text="Setup Instructions" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,10"/>
                <TextBlock TextWrapping="Wrap" Foreground="#FFCCCCCC" FontSize="12" LineHeight="18">
                    <Run Text="1. Install VB-Cable (Virtual Audio Cable) from "/>
                    <Hyperlink NavigateUri="https://vb-audio.com/Cable/" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#FF007ACC">
                        <Run Text="https://vb-audio.com/Cable/"/>
                    </Hyperlink>
                    <LineBreak/>
                    <Run Text="2. Restart your computer after installation"/>
                    <LineBreak/>
                    <Run Text="3. In Discord, set your microphone to 'CABLE Output (VB-Audio Virtual Cable)'"/>
                    <LineBreak/>
                    <Run Text="4. Select 'CABLE Input (VB-Audio Virtual Cable)' as output device below"/>
                    <LineBreak/>
                    <Run Text="5. Start monitoring and use the soundboard - Discord will hear both your mic and sounds!"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Controls Panel -->
        <Border Grid.Row="1" Background="#FF2D2D30" Padding="20,15" BorderBrush="#FF3F3F46" BorderThickness="0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sound Folder Selection -->
                <TextBlock Grid.Row="0" Grid.Column="0" 
                          Text="Sound Folder:" 
                          Foreground="#FFFFFF" 
                          VerticalAlignment="Center" 
                          Margin="0,0,10,0"/>
                
                <TextBox x:Name="FolderPathTextBox" 
                        Grid.Row="0" Grid.Column="1" 
                        Style="{StaticResource ModernTextBoxStyle}"
                        IsReadOnly="True"
                        Margin="0,0,10,0"/>
                
                <Button x:Name="BrowseFolderButton" 
                       Grid.Row="0" Grid.Column="2" 
                       Content="Browse..." 
                       Style="{StaticResource ModernButtonStyle}"
                       Click="BrowseFolderButton_Click"
                       Margin="0,0,10,0"/>

                <!-- Output Device Selection -->
                <TextBlock Grid.Row="1" Grid.Column="0" 
                          Text="Output Device:" 
                          Foreground="#FFFFFF" 
                          VerticalAlignment="Center" 
                          Margin="0,10,10,0"/>
                
                <Grid Grid.Row="1" Grid.Column="1" Margin="0,10,10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ComboBox x:Name="OutputDeviceComboBox" 
                             Grid.Column="0"
                             Style="{StaticResource ModernComboBoxStyle}"
                             SelectionChanged="OutputDeviceComboBox_SelectionChanged"
                             Margin="0,0,5,0"
                             ToolTip="Select your output device. For Discord integration, choose 'CABLE Input (VB-Audio Virtual Cable)'"/>
                    
                    <Button x:Name="RefreshDevicesButton" 
                           Grid.Column="1"
                           Content="↻" 
                           Style="{StaticResource ModernButtonStyle}"
                           Click="RefreshDevicesButton_Click"
                           Width="30"
                           ToolTip="Refresh output devices"/>
                    <Button x:Name="TestButton" 
                           Grid.Column="2"
                           Content="Test" 
                           Style="{StaticResource ModernButtonStyle}"
                           Click="TestButton_Click"
                           Width="50"
                           Margin="5,0,0,0"
                           ToolTip="Test device loading"/>
                </Grid>

                <!-- Microphone Monitoring Option -->
                <CheckBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" x:Name="MonitorMicrophoneCheckBox" 
                          Content="Monitor microphone locally (hear your own voice)" 
                          Foreground="#FFFFFF" Margin="0,10,0,0" 
                          Checked="MonitorMicrophoneCheckBox_Changed" Unchecked="MonitorMicrophoneCheckBox_Changed"/>

                <!-- Control Buttons -->
                <StackPanel Grid.Row="0" Grid.Column="3" Grid.RowSpan="3" 
                           Orientation="Vertical" 
                           VerticalAlignment="Center">
                    <Button x:Name="StartButton" 
                           Content="Start Monitoring" 
                           Style="{StaticResource ModernButtonStyle}"
                           Click="StartButton_Click"
                           Width="120"/>
                    <Button x:Name="StopButton" 
                           Content="Stop Monitoring" 
                           Style="{StaticResource ModernButtonStyle}"
                           Click="StopButton_Click"
                           Width="120"
                           IsEnabled="False"/>
                    <Button x:Name="StopSoundsButton" 
                           Content="Stop All Sounds" 
                           Style="{StaticResource ModernButtonStyle}"
                           Click="StopSoundsButton_Click"
                           Width="120"
                           Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Sound Buttons Area -->
        <ScrollViewer Grid.Row="2" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Disabled"
                     Background="#FF1E1E1E"
                     Padding="20">
            <WrapPanel x:Name="SoundButtonsPanel" 
                      Orientation="Horizontal"
                      HorizontalAlignment="Left"/>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#FF252526" Padding="20,10" BorderBrush="#FF3F3F46" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusTextBlock" 
                          Grid.Column="0"
                          Text="Ready" 
                          Foreground="#FFFFFF" 
                          VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" 
                           Orientation="Horizontal" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock Text="Microphone Level:" 
                              Foreground="#FFFFFF" 
                              Margin="0,0,10,0"/>
                    <ProgressBar x:Name="MicrophoneLevelProgressBar" 
                                Style="{StaticResource ModernProgressBarStyle}"
                                Width="200" 
                                Maximum="1" 
                                Value="0"/>
                </StackPanel>

                <TextBlock x:Name="SoundCountTextBlock" 
                          Grid.Column="2"
                          Text="0 sounds loaded" 
                          Foreground="#FFCCCCCC" 
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>

